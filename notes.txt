
https://docs.docker.com/compose/install/   <----- to install docker-compose
npm i packageName -P   <--- move necessary devDependencies to dependencies Production




CD into ur PEM key directory
sudo chmod 600 pemKeyName.pem   <--- run pem key in ubuntu terminal

git clone your repo onto ubuntu


sudo docker-compose -f docker.compose.yml up build   <---- runs yml script in terminal

sudo docker images   <---- to view imageId


sudo docker container stop containerId  <------ stops container

sudo docker container rm containerId  <---- removes container

sudo docker image rm imageId   <-----   <---- removes imageId

sudo rm -rf directoryName   <----  removes directory

